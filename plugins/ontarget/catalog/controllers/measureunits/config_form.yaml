# ===================================
#  Form Behavior Config
# ===================================

# Record name
name: Measure Unit

# Model Form Field configuration
form: $/ontarget/catalog/models/measureunit/fields.yaml

# Model Class name
modelClass: OnTarget\Catalog\Models\MeasureUnit

# Default redirect location
defaultRedirect: ontarget/catalog/measureunits

# Create page
create:
    title: backend::lang.form.create_title
    redirect: ontarget/catalog/measureunits/update/:id
    redirectClose: ontarget/catalog/measureunits

# Update page
update:
    title: backend::lang.form.update_title
    redirect: ontarget/catalog/measureunits
    redirectClose: ontarget/catalog/measureunits

# Preview page
preview:
    title: backend::lang.form.preview_title
